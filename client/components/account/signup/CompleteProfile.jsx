import React from 'react';
import { connect } from 'react-redux';
import { authenticate } from '../../../store';

function CompleteProfile() {
  return (
    <div className="intro" id="createProfile">
      {' '}
    </div>
  );
}

export default CompleteProfile;
